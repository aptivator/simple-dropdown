export const allConfigs = new Map();

export const arrowNavigationCodes = new Set(['ArrowDown', 'ArrowUp']);

export const arrowToSibling = {
  ArrowUp: 'previousSibling',
  ArrowDown: 'nextSibling'
};

export const defaultPlaceholder = 'Make a selection';

export const defaultValue = '';

export const globalConfigs = {
  scrollBehavior: {
    behavior: 'smooth',
    block: 'nearest'
  },
  triggerImmediately: true
};

export const itemsStore = new Map();

export const libraryName = 'simple-dropdown';

export const libraryNameRef = {libraryName};

export const visibilityMargin = 2;
